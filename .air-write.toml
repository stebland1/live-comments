root = "."
tmp_dir = "tmp"

[build]
  entrypoint = ["./tmp/write-api"]
  cmd = "go build -o ./tmp/write-api ./cmd/write-api"
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go"]
  send_interrupt = true

[log]
  main_only = true
  silent = true
